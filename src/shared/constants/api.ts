import { ApiVersion } from '../types';

export const API_COLLECTION = {
  AUTH: 'authen',
  BACKOFFICE: 'backoffice',
  COMMUNICATION: 'communication',
};

export const API_VERSION_V2: ApiVersion = { version: '2' };
